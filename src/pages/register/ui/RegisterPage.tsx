import { RegisterForm } from "../../../components/RegisterForm";

export const RegisterPage = () => {
  return (
    <section className="flex flex-col items-center justify-center min-h-screen bg-neutral-100">
      <RegisterForm />
    </section>
  );
};
