{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "Before performing any task, always load the relevant skill using the skill tool. Check available skills first, then load the most relevant one for the current task."
  ],
  "skills": {
    "paths": [".agents/skills"]
  },
  "mcp": {
    "heroui-react": {
      "type": "local",
      "command": ["npx", "-y", "@heroui/react-mcp@latest"],
      "enabled": true
    },
    "Pixso MCP": {
      "type": "local",
      "command": ["npx", "-y", "mcp-remote", "http://localhost:3667/mcp"],
      "enabled": true
    }
  }
}
